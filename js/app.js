'use strict';



let min = Math.ceil(min);
let max = Math.floor(max);
let random = Math.floor(Math.random() * (max - min + 1) + min);


let books = [];

function Books(name) {
    this.name
    this.price
    books.push(this)
}